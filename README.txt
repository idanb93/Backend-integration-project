/**
 * Author:    Idan Bardugo
 * Created:   23.09.2021
 **/

Instructions:

1. Run the CycleService script
2. The program is not interactive due to issues i had with returning the value from the subprocess as stdout.
   you might want/need to change values of the ConnectorSettings(interval, script_path, connector_name, output_path) or
   SubProcessInputOutputHandler(source_folder, iteration_entities_count) Please do this manually (hardcoded).